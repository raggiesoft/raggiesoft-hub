# =================================================
# RAGGIESOFT HUB - GIT IGNORE PROTOCOLS
# Context: PHP (Elara) + C# (Silas) + Web
# =================================================

# --- 1. SECURITY (CRITICAL) ---
# Never commit environment variables, keys, or secrets.
.env
.env.local
*.pem
*.key

# --- 2. C# / .NET (Silas) ---
# Ignore build output (binaries) and intermediate files.
# The pattern [Bb]in/ catches both "bin/" and "Bin/" case variations.
_sysops/Silas/[Bb]in/
_sysops/Silas/[Oo]bj/

# --- 3. PHP / COMPOSER ---
# Ignore 3rd party libraries. We only commit composer.json/.lock
vendor/
composer.phar

# --- 4. WEB / JAVASCRIPT ---
# If you use Node/NPM for frontend assets later
node_modules/
npm-debug.log

# --- 5. IDE & EDITOR SETTINGS ---
# Personal preference files that shouldn't be shared.
.idea/
.vscode/
*.swp
*.suo
*.user
.vs/

# --- 6. OS GENERATED FILES ---
# Garbage left by operating systems.
.DS_Store
Thumbs.db
Desktop.ini
ehthumbs.db

# --- 7. LARGE MEDIA SAFEGUARDS ---
# The Hub is for code. Media goes in raggiesoft-assets.
# These rules prevent accidental commits of heavy files to the Code repo.
*.zip
*.tar.gz
*.rar
*.iso
*.mp3
*.wav
*.flac
*.mp4
*.mov